## Requirements

> This capability has been superseded by the WebRTC audio transport (see `webrtc-audio-transport`).
> The binary audio relay pipeline was removed in the audio-webrtc-migration change.
